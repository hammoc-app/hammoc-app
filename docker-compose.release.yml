# This file is for testing Docker images only. Do not run in production!
# Use:
#   docker-compose -f docker-compose.release.yml
---
version: '3'

services:
  hammoc:
    build:
      dockerfile: Dockerfile.release
      context: .
    links:
      - postgresql
    ports:
      - '8080:8080'
    environment:
      PORT: 8080
      SITE_HOST: hammoc.app
      SITE_PORT: 443
      SITE_SCHEME: https
      SECRET_KEY_BASE: QsWL9jJw00QsH/6UnZiCXzDgha0JFsoeE6qi43aN967m8lUsM4O5B4U7G6tPTDVy
      DB_URL: ecto://postgres:postgres@postgresql/hammoc_prod
      TWITTER_CONSUMER_KEY: "${TWITTER_CONSUMER_KEY}"
      TWITTER_CONSUMER_SECRET: "${TWITTER_CONSUMER_SECRET}"
      TWITTER_ACCESS_TOKEN: "${TWITTER_ACCESS_TOKEN}"
      TWITTER_ACCESS_TOKEN_SECRET: "${TWITTER_ACCESS_TOKEN_SECRET}"
      ERTS_COOKIE: mycookiebringzallboyztotheyarrrd

  postgresql:
    image: postgres:11.1-alpine
    restart: always
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: hammoc_prod
    ports:
      - '5432:5432'
